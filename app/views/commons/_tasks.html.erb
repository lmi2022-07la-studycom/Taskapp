<table>
    <tr>
        <th>id</th><th>タスク内容</th><th>ステータス</th><th>登録日時</th>
    </tr>
    <% @tasks.each do |task| %>
    <tr>
        <td><%= task.id %></td>
        <td><%= task.title %></td>
        <td><%= task.status %></td>
        <td><%= task.created_at.strftime('%Y/%m/%d %H時%M分%S秒') %></td>
        <td><%= link_to '編集', edit_task_path(task) %></td>
        <td><%= link_to '削除', task_path(task), method: :delete, data: {confirm: '削除しますか？'} %></td>
    </tr>
    <% end %>
</table>